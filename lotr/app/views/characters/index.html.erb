<h1>LOTR Characters</h1>
<%= link_to "Sort by Name", order_path%>
 <%@characters.each do |character|%>
    <p><%=link_to character.name, character_path(character.id)%></p>
 <%end%>
 <p><%=link_to "Add a Character", new_character_path%></p>

